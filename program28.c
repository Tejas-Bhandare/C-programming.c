#include <stdio.h>

int main()
{

  int iCounter = 0;

  int iFrequency = 0;
  printf("Enter the frequency \n");
  scanf("%d", &iFrequency);
  
  for (iCounter = 1; iCounter <= iFrequency; iCounter++)
  {
    printf("Jai ganesh ...... !\n");
  }

  return 0;
}